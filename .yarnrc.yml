# Equivalent to --frozen-lockfile true from .yarnrc
enableImmutableInstalls: true

# Registry configuration (equivalent to registry "https://registry.yarnpkg.com")
npmRegistryServer: "https://registry.yarnpkg.com"

# Additional recommended settings for Yarn 4
nodeLinker: node-modules
enableGlobalCache: true
compressionLevel: mixed
